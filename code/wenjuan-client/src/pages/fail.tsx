import React, { memo } from "react";

// eslint-disable-next-line react/display-name
const QuestionFail = memo(() => {
  return (
    <>
      <p>问卷提交失败</p>
    </>
  );
});

export default QuestionFail;
