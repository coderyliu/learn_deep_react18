import React, { memo } from 'react'

import { AppLoginWrapper } from './style'

const AppLogin = memo(() => {
  return (
    <AppLoginWrapper>
      <h2>登录页</h2>
    </AppLoginWrapper>
  )
})

export default AppLogin